---
- name: Pre-deployment validation
  hosts: all
  gather_facts: true
  become: false
  tags: [validation, pre_validation]
  tasks:
    - name: Run backup validation for all hosts
      ansible.builtin.include_role:
        name: common
        tasks_from: backup_validation
      tags: [backup_validation]

    - name: Run role-specific validation
      tags: [role_validation]
      block:
        - name: DNS validation for Pi-hole hosts
          ansible.builtin.include_role:
            name: dns
            tasks_from: validate
          when: "'dns' in group_names"

        - name: Automation validation for automation hosts
          ansible.builtin.include_role:
            name: automation
            tasks_from: validate
          when: "'automation' in group_names"

        - name: Music stack validation for music hosts
          ansible.builtin.include_role:
            name: music-stack
            tasks_from: validate
          when: "'music' in group_names"

        - name: Monitoring validation for monitoring hosts
          ansible.builtin.include_role:
            name: monitoring
            tasks_from: validate
          when: "'monitoring' in group_names"

- name: Configure all hosts with common setup
  hosts: all
  become: true
  gather_facts: true
  pre_tasks:
    - name: Update package cache
      ansible.builtin.include_role:
        name: common
        tasks_from: package_cache_update

  roles:
    - role: common
      tags: [common, base]
    - role: firewall
      tags: [firewall, security]
    - role: prometheus-exporters
      tags: [monitoring, prometheus, exporters]

  post_tasks:
    - name: Verify base system configuration
      ansible.builtin.systemd:
        name: "{{ item }}"
        state: started
        enabled: true
      loop:
        - fail2ban
        - node-exporter
      tags: [verification]

- name: Configure DNS servers
  hosts: dns
  become: true
  gather_facts: true
  roles:
    - role: dns
      tags: [dns, pihole]

- name: Configure music stack
  hosts: music
  become: true
  gather_facts: true
  roles:
    - role: music-stack
      tags: [music, docker]

- name: Configure automation stack
  hosts: automation
  become: true
  gather_facts: true
  roles:
    - role: automation
      tags: [automation, traefik]

- name: Configure monitoring infrastructure
  hosts: monitoring
  become: true
  gather_facts: true
  roles:
    - role: monitoring
      tags: [monitoring, grafana, prometheus, loki]

- name: Configure backup across all infrastructure hosts
  hosts: services,monitoring
  become: true
  gather_facts: true
  roles:
    - role: backup
      tags: [backup]
