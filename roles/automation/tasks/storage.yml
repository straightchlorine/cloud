---
# Storage setup for automation stack - using common storage logic

- name: Include common storage setup  # noqa: var-naming[no-role-prefix]
  ansible.builtin.include_role:
    name: common
    tasks_from: storage
  vars:
    mount_path: "{{ automation_data_path }}"
