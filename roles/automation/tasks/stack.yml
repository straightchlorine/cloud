---
# Compose contract: secrets live ONLY in the vault env dict below, rendered by
# common's build_docker_compose as a 0600 .env next to docker-compose.yml and
# interpolated by compose via ${VAR} - the compose file never carries a secret.

- name: Resolve the stack user's uid/gid on this host
  # CWA honors PUID/PGID and must not run as root (its library files then end
  # up root-owned, which breaks ingestion/transfer). Do NOT use the
  # ansible_user_dir / ansible_user_uid FACTS here - under become:true they
  # reflect root (the /root automation_stack_home trap). Resolve the
  # inventory's ansible_user on this host instead.
  ansible.builtin.getent:
    database: passwd
    key: "{{ ansible_user }}"

- name: Publish the stack user's uid/gid for the compose services
  ansible.builtin.set_fact:
    automation_stack_uid: "{{ getent_passwd[ansible_user][1] }}"
    automation_stack_gid: "{{ getent_passwd[ansible_user][2] }}"

- name: Create the stack data directories
  ansible.builtin.file:
    path: "{{ automation_data_path }}/{{ item }}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"
  become: true
  loop:
    - vaultwarden
    - firefly
    - calibre/config
    - calibre/ingest
    - calibre/library

- name: Create the MariaDB data directory owned by Docker's userns-remap range
  # mariadb's entrypoint chown -Rs /var/lib/mysql to mysql:mysql as root on
  # every start. Under userns-remap, container root is common_dockremap_uid/gid,
  # not real host root - it has no authority to chown a directory owned by
  # ansible_user, so this dir (and anything already inside it, left over from
  # a teardown that intentionally preserves automation_data_path) must be
  # owned by the remap range itself.
  ansible.builtin.file:
    path: "{{ automation_data_path }}/mariadb"
    state: directory
    owner: "{{ common_dockremap_uid }}"
    group: "{{ common_dockremap_gid }}"
    mode: "0755"
    recurse: true
  become: true

- name: Create the Firefly upload directory owned by the image's www-data
  # fireflyiii/core runs php-fpm as USER www-data (uid/gid 33 on its Debian
  # base) and never chowns its storage. Upstream's compose gets away with a
  # named volume, which inherits the image's ownership; a bind mount starts
  # out owned by the host user and Firefly then fails attachment uploads
  # SILENTLY ("storage/upload is not writeable" in the log, HTTP 200 on the
  # attachment view, zero bytes on disk).
  # ponytail: uid pinned to 33 because the image is Debian-based - if upstream
  # ever moves to their Alpine variant this becomes 82.
  ansible.builtin.file:
    path: "{{ automation_data_path }}/firefly/upload"
    state: directory
    owner: "33"
    group: "33"
    mode: "0755"
  become: true

- name: Create the stack home directory
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"
  become: true
  loop:
    - "{{ automation_stack_home }}"
    - "{{ automation_stack_home }}/scripts"

- name: Render compose + .env and bring up the stack
  ansible.builtin.include_role:
    name: common
    tasks_from: deploy_compose_stack
  vars:  # noqa: var-naming[no-role-prefix]
    stack_name: "automation"
    compose_file_path: "{{ automation_stack_home }}/docker-compose.yml"
    compose_owner: "{{ ansible_user }}"
    compose_services: "{{ automation_compose_services }}"
    compose_networks: "{{ automation_compose_networks }}"
    compose_volumes: "{{ automation_compose_volumes }}"
    # firefly-cron is deliberately NOT in the critical set: it is a sleep-sidecar
    # (wget the cron endpoint -> sleep 60s -> exit 0 -> Docker restarts it), so it
    # only reports State=running ~60s of every ~62s cycle. Requiring a running
    # state would false-fail every deploy exactly when the sidecar is healthy.
    # Its health is proven separately in post_deploy_validate.yml by the LAST
    # exit code (0 = the wget+sleep cycle completed cleanly).
    compose_critical_services:
      - vaultwarden
      - firefly
      - mariadb
      - watchtower
    # Vault glue - the only place real secret values exist in the role. The
    # .env file also feeds Watchtower's ntfy notifications (group_vars/all).
    compose_env_vars:
      FIREFLY_APP_KEY: "{{ vault_firefly_app_key }}"
      FIREFLY_STATIC_CRON_TOKEN: "{{ vault_firefly_static_cron_token }}"
      FIREFLY_DB_PASSWORD: "{{ vault_firefly_db_password }}"
      VAULTWARDEN_ADMIN_TOKEN: "{{ vault_vaultwarden_admin_token }}"
      WATCHTOWER_API_TOKEN: "{{ vault_watchtower_api_token }}"
      # Optional Hardcover metadata-provider key (CWA env). Empty until
      # vault_hardcover_token is set in vault, so molecule/test deploys without
      # the vault var still render.
      HARDCOVER_TOKEN: "{{ vault_hardcover_token if vault_hardcover_token is defined else '' }}"
      # Watchtower notifications ride the shared ntfy topic when a key is
      # configured (the key deploy is optional in common/ntfy_install.yml);
      # an empty URL just leaves watchtower silent. shoutrrr's ntfy URL is
      # ntfy://username:password@host/topic and ntfy only honours a token as
      # the basic-auth PASSWORD (the leading ':' keeps the username empty) -
      # a key in the username slot is rejected with ntfy error 40301
      # (forbidden). This differs from the Ansible notifier, which sends the
      # same key as a Bearer header and is accepted.
      WATCHTOWER_NOTIFICATION_URL: >-
        {{ 'ntfy://:' ~ vault_ntfy_api_key ~ '@' ~
        (ntfy_server_url | regex_replace('^https?://', '')) ~ '/' ~ ntfy_topic
        if vault_ntfy_api_key is defined else '' }}
    start_stack: "{{ common_start_stack }}"
  # No notify: deploy_compose_stack's `docker compose up -d` already applies
  # file changes by recreating containers - a restart handler would only
  # double-restart the stack on every config change.

- name: Install the stack management script
  ansible.builtin.template:
    src: "{{ role_path }}/../common/templates/manage-stack.sh.j2"
    dest: "{{ automation_stack_home }}/scripts/manage-automation.sh"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"
  vars:
    manage_stack_home: "{{ automation_stack_home }}"
    manage_stack_name: "automation"

- name: Symlink the management script into PATH
  ansible.builtin.file:
    src: "{{ automation_stack_home }}/scripts/manage-automation.sh"
    dest: /usr/local/bin/manage-automation
    state: link
    force: true
  become: true
