---
# docker_stack_service's unit template notifies "Restart {{ stack_service_name }}"
# whenever the unit file changes (first deploy or unit edit).
- name: Restart stack
  ansible.builtin.systemd:
    name: stack
    state: restarted
    daemon_reload: true
  become: true
