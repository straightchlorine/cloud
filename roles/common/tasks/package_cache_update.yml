---
# Standardized package cache update
# Used by all playbooks to eliminate pre-task duplication

- name: Update package cache
  ansible.builtin.apt:
    common_update_cache: true
    common_cache_valid_time: "{{ package_cache_valid_time }}"
  tags: [always, packages]
