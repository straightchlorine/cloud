---
role_name_check: 0
driver:
  name: Docker
platforms:
  - name: Debian12-hardware
    image: ${MOLECULE_IMAGE:-docker.io/geerlingguy/docker-debian12-ansible:latest}
    pre_build_image: true
    ansible.builtin.command: ${MOLECULE_COMMAND:-/lib/systemd/systemd}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
provisioner:
  name: Ansible
  inventory:
    group_vars:
      all:
        timezone: UTC
verifier:
  name: Ansible
scenario:
  name: Hardware-detection
  description: Test hardware platform detection and resource limit calculation
