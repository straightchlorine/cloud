---
# Molecule configuration for Docker installation testing
# Tests Docker setup with user namespace mapping for security

driver:
  name: Docker

platforms:
  - name: Debian12-docker
    image: docker.io/geerlingguy/docker-debian12-ansible:latest
    pre_build_image: true
    ansible.builtin.command: /lib/systemd/systemd
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    # Note: Privileged required for Docker-in-Podman testing
    privileged: true
    capabilities:
      - SYS_ADMIN
    tmpfs:
      - /run
      - /tmp
    environment:
      container: podman

provisioner:
  name: Ansible
  config_options:
    defaults:
      interpreter_python: auto_silent
  inventory:
    group_vars:
      all:
        ansible_user: root
        timezone: UTC
        package_cache_valid_time: 3600

verifier:
  name: Ansible

scenario:
  name: Docker-installation
  description: Test Docker installation with user namespace mapping (userns-remap)
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - verify
    - idempotence
    - cleanup
    - destroy
