---
# SSD mounting and storage setup - using common storage logic

- name: Include common storage setup
  ansible.builtin.include_role:
    name: Common
    tasks_from: storage
  vars:
    mount_path: "{{ music_stack_music_library_path }}"
    mount_subdirectories: "{{ music_stack_music_library_directories }}"
