---
# Ansible Galaxy collections required for testing and deployment
# Install: ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansible.utils
    version: ">=2.10.0"

  - name: community.general
    version: ">=7.0.0"

  - name: containers.podman
    version: ">=1.10.0"
